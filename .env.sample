# e.g. http://127.0.0.1:8001 — do not use http://0.0.0.0:8001 in the browser (use 127.0.0.1 or localhost)
REACT_APP_BACKEND_API_ENDPOINT=""
REACT_APP_ENVIRONMENT="dev"
REACT_APP_MINIQUIZ_ENDPOINT = ""
# Backend must allow this origin in CORS for miniquiz fetch() to POST .../completions (see docs/shared/contracts/auth-and-profile-contract.md §6.1)